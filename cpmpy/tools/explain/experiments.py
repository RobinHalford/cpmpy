# file that initiates experiments for diverse MUS enumeration
import cpmpy as cp
import numpy as np
import importlib
importlib.reload(cp)
from cpmpy.tools.explain.marco import marco
from cpmpy.tools.explain.utils import diversity_matrix
from cpmpy.tools.explain.diverse_enumeration import marco_select_top_k, marco_diverse, marco_until_diverse, ocus_enum
import warnings
warnings.simplefilter(action='ignore', category=FutureWarning)
warnings.simplefilter(action='ignore', category=UserWarning)
from cpmpy.tools.explain.visualize_diversity import visualize_heatmap


def create_sudoku_constraints(given):
    """
    Create a CPMpy model for the given Sudoku puzzle.
    """
    puzzle = cp.intvar(1,9, shape=given.shape, name="puzzle")
    hard = []
    soft = []
    soft.append(puzzle[given!=e] == given[given!=e])
    hard.append([cp.AllDifferent(row) for row in puzzle])
    hard.append([cp.AllDifferent(col) for col in puzzle.T])
    # Constraints on blocks
    for i in range(0,9, 3):
        for j in range(0,9, 3):
            hard.append(cp.AllDifferent(puzzle[i:i+3, j:j+3])) # python's indexing
    return puzzle, hard, soft

e = 0
given = np.array([
    [e, 2, e,  2, e, 5,  e, e, e],
    [e, 2, e,  e, 2, e,  7, 3, e],
    [e, e, 2,  e, e, 9,  e, 6, e],

    [2, e, e,  e, e, 2,  4, e, 9],
    [e, 2, e,  e, 7, 2,  e, 2, e],
    [6, e, 9,  e, e, e,  e, e, 1],

    [e, 8, e,  4, e, 2,  1, 2, e],
    [e, 6, 3,  e, 2, e,  e, 8, e],
    [e, e, 2,  6, e, 8,  e, e, e]])

_, hard, soft = create_sudoku_constraints(given)

muses = []
for i, (kind, subset) in enumerate(marco(soft=soft, hard=hard, solver="exact", map_solver="exact", return_mcs=False)):
    muses.append(frozenset(subset))
    if i == 19:
        break

div_matrix = diversity_matrix(muses, measure="overlap")
avg = div_matrix[np.triu_indices_from(div_matrix, k=1)].mean()
print(f"The average diversity of {len(muses)} MUSes generated by MARCO is: {avg}")


muses = []
for i, (kind, subset) in enumerate(marco_diverse(soft=soft, hard=hard, solver="exact", map_solver="exact", return_mcs=False)):
    muses.append(frozenset(subset))
    if i == 19:
        break

div_matrix = diversity_matrix(muses, measure="overlap")
avg = div_matrix[np.triu_indices_from(div_matrix, k=1)].mean()
print(f"The average diversity of {len(muses)} MUSes generated by D-MARCO is: {avg}")


